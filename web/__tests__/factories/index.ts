/**
 * Central export point for all test factories.
 *
 * Import factories from here in your tests:
 * @example
 * ```typescript
 * import { foodFactory, userFactory, mealFactory } from '../factories';
 *
 * const food = foodFactory.build();
 * const user = await userFactory.create();
 * const meal = await mealFactory.create();
 * ```
 */
export { userFactory } from "./user.factory";
