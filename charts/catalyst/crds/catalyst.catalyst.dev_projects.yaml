---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: projects.catalyst.catalyst.dev
spec:
  group: catalyst.catalyst.dev
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project is the Schema for the projects API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Project
            properties:
              resources:
                description: Resources configuration (quotas, limits)
                properties:
                  defaultQuota:
                    description: DefaultQuota defines the default resource quota for
                      environments of this project
                    properties:
                      cpu:
                        description: CPU limit (e.g. "1")
                        type: string
                      memory:
                        description: Memory limit (e.g. "2Gi")
                        type: string
                    type: object
                type: object
              sources:
                description: Sources configuration for the project (supports multiple
                  repos)
                items:
                  properties:
                    branch:
                      description: Branch is the default branch to use
                      type: string
                    name:
                      description: Name to identify this source component (e.g. "frontend",
                        "backend")
                      type: string
                    repositoryUrl:
                      description: RepositoryURL is the git repository URL
                      type: string
                  required:
                  - branch
                  - name
                  - repositoryUrl
                  type: object
                type: array
              templates:
                additionalProperties:
                  properties:
                    path:
                      description: Path to the deployment definition (e.g. chart path)
                      type: string
                    sourceRef:
                      description: |-
                        SourceRef refers to one of the sources defined in Project.Sources
                        If empty, assumes the first source or a primary source.
                      type: string
                    type:
                      description: Type of deployment (helm, manifest, kustomize)
                      type: string
                    values:
                      description: Values are the default values to inject
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - type
                  type: object
                description: Templates for different environment types (e.g., "development",
                  "production")
                type: object
            required:
            - sources
            type: object
          status:
            description: status defines the observed state of Project
            properties:
              conditions:
                description: conditions represent the current state of the Project
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
