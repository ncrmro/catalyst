---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: environments.catalyst.catalyst.dev
spec:
  group: catalyst.catalyst.dev
  names:
    kind: Environment
    listKind: EnvironmentList
    plural: environments
    singular: environment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Environment is the Schema for the environments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Environment
            properties:
              config:
                description: Config overrides
                properties:
                  envVars:
                    description: EnvVars to inject into the deployment
                    items:
                      properties:
                        name:
                          type: string
                        value:
                          type: string
                      required:
                      - name
                      - value
                      type: object
                    type: array
                  image:
                    description: |-
                      Image is the container image to deploy (e.g., "ghcr.io/ncrmro/catalyst:latest")
                      If not specified, defaults to cluster registry with commit SHA
                    type: string
                type: object
              deploymentMode:
                description: |-
                  DeploymentMode specifies how the operator should deploy this environment.
                  Valid values: "production", "development", "workspace" (default).
                  - "production": Static deployment from manifest pattern
                  - "development": Hot-reload with volume mounts and init containers
                  - "workspace": Simple workspace pod (default, existing behavior)
                type: string
              projectRef:
                description: ProjectRef references the parent Project
                properties:
                  name:
                    description: Name of the project CR
                    type: string
                required:
                - name
                type: object
              sources:
                description: Sources configuration for this specific environment
                items:
                  properties:
                    branch:
                      description: Branch name
                      type: string
                    commitSha:
                      description: CommitSha is the git commit to deploy
                      type: string
                    name:
                      description: Name identifies the component (matches Project.Sources[].Name)
                      type: string
                    prNumber:
                      description: PrNumber (optional) for pull requests
                      type: integer
                  required:
                  - branch
                  - commitSha
                  - name
                  type: object
                type: array
              type:
                description: Type of environment (development, deployment)
                type: string
            required:
            - projectRef
            - sources
            - type
            type: object
          status:
            description: status defines the observed state of Environment
            properties:
              conditions:
                description: conditions represent the current state of the Environment
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              phase:
                description: Phase represents the current lifecycle state (Pending,
                  Building, Deploying, Ready, Failed)
                type: string
              url:
                description: URL is the public endpoint if available
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
