---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: projects.catalyst.catalyst.dev
spec:
  group: catalyst.catalyst.dev
  names:
    kind: Project
    listKind: ProjectList
    plural: projects
    singular: project
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Project is the Schema for the projects API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Project
            properties:
              githubInstallationId:
                description: |-
                  GitHubInstallationId is the identifier used by the credential helper for
                  this project.
                  
                  In normal operation, this should be the numeric GitHub App installation ID
                  assigned when the GitHub App is installed on a repository or organization
                  (represented as a string). In development mode, the special literal value
                  "pat" is also supported to indicate that a GitHub personal access token
                  should be used instead of a GitHub App installation.
                  
                  This value is used by the credential helper to fetch fresh GitHub tokens
                  for git operations.
                type: string
              resources:
                description: Resources configuration (quotas, limits)
                properties:
                  defaultQuota:
                    description: DefaultQuota defines the default resource quota for
                      environments of this project
                    properties:
                      cpu:
                        description: CPU limit (e.g. "1")
                        type: string
                      memory:
                        description: Memory limit (e.g. "2Gi")
                        type: string
                    type: object
                type: object
              sources:
                description: Sources configuration for the project (supports multiple
                  repos)
                items:
                  properties:
                    branch:
                      description: Branch is the default branch to use
                      type: string
                    name:
                      description: Name to identify this source component (e.g. "frontend",
                        "backend")
                      type: string
                    repositoryUrl:
                      description: RepositoryURL is the git repository URL
                      type: string
                  required:
                  - branch
                  - name
                  - repositoryUrl
                  type: object
                type: array
              templates:
                additionalProperties:
                  properties:
                    builds:
                      description: |-
                        Builds defines the artifacts (container images) to be built from sources before deployment.
                        This separates the "deployment logic" (e.g. Helm Chart) from the "application code" (e.g. Dockerfiles).
                        The operator will build these images and can inject them into the deployment (e.g. via Helm values).
                      items:
                        properties:
                          dockerfile:
                            description: |-
                              Dockerfile is the path to the Dockerfile relative to Path.
                              If empty, auto-detection or default "Dockerfile" is assumed.
                            type: string
                          name:
                            description: |-
                              Name identifies this build artifact (e.g. "frontend", "api").
                              This name is used to inject the built image into the deployment values.
                              Example: for name "frontend", values might receive global.images.frontend.repository
                            type: string
                          path:
                            description: |-
                              Path is the build context directory relative to the SourceRef root.
                              Defaults to root if empty.
                            type: string
                          resources:
                            description: Resources allows customizing the build job
                              resources (requests/limits)
                            properties:
                              claims:
                                description: |-
                                  Claims lists the names of resources, defined in spec.resourceClaims,
                                  that are used by this container.

                                  This field depends on the
                                  DynamicResourceAllocation feature gate.

                                  This field is immutable. It can only be set for containers.
                                items:
                                  description: ResourceClaim references one entry
                                    in PodSpec.ResourceClaims.
                                  properties:
                                    name:
                                      description: |-
                                        Name must match the name of one entry in pod.spec.resourceClaims of
                                        the Pod where this field is used. It makes that resource available
                                        inside a container.
                                      type: string
                                    request:
                                      description: |-
                                        Request is the name chosen for a request in the referenced claim.
                                        If empty, everything from the claim is made available, otherwise
                                        only the result of this request.
                                      type: string
                                  required:
                                  - name
                                  type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                - name
                                x-kubernetes-list-type: map
                              limits:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Limits describes the maximum amount of compute resources allowed.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                              requests:
                                additionalProperties:
                                  anyOf:
                                  - type: integer
                                  - type: string
                                  pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                  x-kubernetes-int-or-string: true
                                description: |-
                                  Requests describes the minimum amount of compute resources required.
                                  If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                  otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                  More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                                type: object
                            type: object
                          sourceRef:
                            description: SourceRef refers to the Project.Source containing
                              the application code.
                            type: string
                        required:
                        - name
                        - sourceRef
                        type: object
                      type: array
                    path:
                      description: Path to the deployment definition (e.g. chart path)
                        relative to SourceRef root.
                      type: string
                    sourceRef:
                      description: |-
                        SourceRef refers to one of the sources defined in Project.Sources
                        containing the deployment configuration (e.g. Chart.yaml or k8s manifests).
                        If empty, assumes the first source or a primary source.
                      type: string
                    type:
                      description: Type of deployment (helm, manifest, kustomize)
                      type: string
                    values:
                      description: Values are the default values to inject
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - type
                  type: object
                description: |-
                  Templates for different environment types.
                  Standard keys: "development" (previews) and "deployment" (production/staging).
                type: object
            required:
            - sources
            type: object
          status:
            description: status defines the observed state of Project
            properties:
              conditions:
                description: conditions represent the current state of the Project
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
